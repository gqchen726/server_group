server:
  port: ${PORT:9011}
  servlet:
    context-path: /
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8000/eureka/
spring:
  application:
    name: server-file
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 100MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ���ز���
    url: jdbc:${CLEARDB_DATABASE_URL:mysql://tianyuge_tool:tianyuge397#(&@db4free.net/tianyuge_tool_db?reconnect=true}&serverTimezone=UTC&characterEncoding=utf-8
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  redis:
    url: ${REDIS_URL:redis://:paaaeaf561dfd8761ec41b08eeb065d1f8a33c270c13e0ddb5a6750d861cc1245@ec2-3-226-202-108.compute-1.amazonaws.com:23709}
    database: 3